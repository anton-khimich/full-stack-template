name: Deploy
on:
  push:
    branches:
      - main

jobs:
  pre_deploy:
    uses: ./.github/workflows/verify_build.yml
  deploy:
    needs: pre_deploy
    uses: ./.github/workflows/cloudflare_pages_deploy.yml
